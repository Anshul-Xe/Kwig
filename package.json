{
  "name": "daily-tracker-mobile",
  "version": "1.0.0",
  "description": "Notion-inspired mobile productivity tracker",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
